[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eowm"
version = "0.1.0"
description = "Reference implementation of EO provenance protection via self-recalibrated watermarking."
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "pandas>=2.0"]
